spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskflow_db
    username: root
    password: MySql@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

app:
  security:
    jwt:
      secret-key: 12km2n3xdeugvhs&ehs%jefc$rt#@k4e
      expiration: 86400000
    cors:
      allowed-origins: http://localhost:5173
  swagger:
    contact:
      name: taskflow
      email: taskflow@email.com